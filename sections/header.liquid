<div class='header__wrapper header_sticky-{{ section.settings.header_sticky }} border-b-[{{ section.settings.border_width }}px]'>
  <div class='flex items-center justify-between my-3{% if section.settings.header_width == "full" %} mx-3{% else %} mx-2 lg:container lg:mx-auto{% endif %}'>
    <div class='header__mobile_nav block lg:hidden'>
      {% render 'icons',
        icons: 'icon-hamburger',
        icon_classes: 'cursor-pointer',
        icon_height: '22px',
        icon_width: '22px',
        icon_color: '#ffffff'
      %}
    </div>
    <div
      class='header__logo'
      style='--header-logo-height-mobile:{{ settings.logo_height_mb }}px;--header-logo-height-desktop:{{ settings.logo_height_desk }}px'
    >
      {% if settings.logo != blank %}
        <a href='{{ shop.url }}'>
          <img
            src='{{ settings.logo | image_url }}'
            alt='{{ settings.logo.alt | default: shop.name }}'
            class='header__logo'
            height='auto'
            width='auto'
          >
        </a>
      {% else %}
        <h1 class='header__title'>{{ shop.name }}</h1>
      {% endif %}
    </div>
    <div class='header__nav hidden lg:block'>
      <nav class='navigation__bar'>
        <ul class='flex gap-3'>
          {% for link in section.settings.menu.links %}
            <li>
              <a href='{{ link.url }}'>{{ link.title }}</a>
            </li>
          {% endfor %}
        </ul>
      </nav>
    </div>
    <div class='header__feature_icons flex gap-3'>
      {% if section.settings.show_search %}
        {% render 'icons',
          icons: 'icon-search',
          icon_classes: 'cursor-pointer tooltip',
          icon_height: '22px',
          icon_width: '22px',
          icon_color: '#ffffff',
          icon_attributes: 'data-tippy-content="Search the store" data-tippy-placement="bottom"'
        %}
      {% endif %}
      <a href='{{ routes.account_profile_url }}'>
        {% render 'icons',
          icons: 'icon-account',
          icon_classes: 'cursor-pointer tooltip',
          icon_height: '22px',
          icon_width: '22px',
          icon_color: '#ffffff',
          icon_attributes: 'data-tippy-content="View your account" data-tippy-placement="bottom"'
        %}
      </a>
      {% render 'icons',
        icons: 'icon-cart',
        icon_classes: 'cursor-pointer tooltip',
        icon_height: '22px',
        icon_width: '22px',
        icon_color: '#ffffff',
        icon_attributes: 'data-tippy-content="View your cart" data-tippy-placement="bottom"'
      %}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Header",
  "tag": "header",
  "class": "section-header",
  "settings": [
    {
      "type": "header",
      "content": "Menu Bar"
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "Menu"
    },
    {
      "type": "header",
      "content": "Search Bar"
    },
    {
      "type": "checkbox",
      "id": "show_search",
      "label": "Show Search"
    },
    {
      "type": "header",
      "content": "Appearance"
    },
    {
      "type": "select",
      "id": "header_width",
      "label": "Width",
      "options": [
        {
          "label": "Page",
          "value": "container"
        },
        {
          "label": "Full",
          "value": "full"
        }
      ]
    },
    {
      "type": "select",
      "id": "header_sticky",
      "label": "Sticky header",
      "options": [
        {
          "label": "Always",
          "value": "always"
        },
        {
          "label": "On scroll up",
          "value": "on_scroll_up"
        },
        {
          "label": "Never",
          "value": "never"
        }
      ]
    },
    {
      "type": "range",
      "id": "border_width",
      "label": "Border thickness",
      "min": 0,
      "max": 5,
      "step": 1,
      "unit": "px",
      "default": 1
    }
  ],
  "presets": [
    {
      "name": "Header",
      "category": "Header"
    }
  ]
}
{% endschema %}
